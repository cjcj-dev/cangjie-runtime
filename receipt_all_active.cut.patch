diff --git a/runtime/src/Heap/Collector/Generation.cpp b/runtime/src/Heap/Collector/Generation.cpp
--- a/runtime/src/Heap/Collector/Generation.cpp
+++ b/runtime/src/Heap/Collector/Generation.cpp
@@ -819,10 +819,6 @@ void WCollector::DoYoungGarbageCollection()
         // minortime: ③ root enum (alloc buffers + VisitMinorRoots)
         MRT_PHASE_TIMER("young.root_enum");
         WorkStack enumRoots = NewWorkStack();
-        theAllocator.VisitAllocBuffers([&enumRoots](AllocBuffer& buffer) { buffer.MergeRoots(enumRoots); });
-        if (stackScanEpoch != 0) {
-            SatbBuffer::Instance().GetRetiredObjects(enumRoots);
-        }
         while (!enumRoots.empty()) {
             const MarkStackEntry entry = enumRoots.back();
             BaseObject* object = entry.object();
@@ -886,7 +882,6 @@ void WCollector::DoYoungGarbageCollection()
         // wave8 y2y handoff (8d4253522 content): consume the pre-window batch
         // before reset releases mutators. New stores after reset remain owned
         // by the STW2 consumer and cannot race this allocator-buffer merge.
-        mergeY2yDirtyHolders(workStack);
 #if defined(MRT_TESTABLE_INTERNALS)
         NoteY2yBeforeReleaseTestReceipt(pendingY2yDirtyHolderCount());
 #endif
@@ -994,8 +989,6 @@ void WCollector::DoYoungGarbageCollection()
             // publication. Late stack roots are mark work, just like ZGC's
             // thread-local mark stacks flushed by ZMark::try_end().
             buffer.MergeRoots(workStack);
-            buffer.MergeYoungAllocBlack(workStack);
-            buffer.MergeY2yDirtyHolders(workStack);
         });
 #if defined(MRT_TESTABLE_INTERNALS)
         NoteY2yAfterStw2TestReceipt(y2yBatchAtMarkEnd);
diff --git a/runtime/src/Heap/Collector/Mark.cpp b/runtime/src/Heap/Collector/Mark.cpp
--- a/runtime/src/Heap/Collector/Mark.cpp
+++ b/runtime/src/Heap/Collector/Mark.cpp
@@ -2130,6 +2130,7 @@ bool WCollector::MarkYoungSatbBuffer(WorkStack& workStack, bool fullYoungScan, M
     // window do GC work", not "how many greys survived the filter".
     size_t satbSeen = 0;
     auto visitSatbObj = [this, &workStack, windowStats, &satbSeen]() {
+        return; // fault arm: disconnect concurrent young retired-receipt consumer
         WorkStack remarkStack;
         SatbBuffer::Instance().GetRetiredEntries([&](BaseObject* obj, bool follow) {
             ++satbSeen;
@@ -2184,6 +2185,7 @@ bool WCollector::TryEndYoungMark(WorkStack& workStack, YoungConcWindowStats* win
     NoteMarkTerminatePause();
     size_t flushed = 0;
     MutatorManager::Instance().VisitAllMutators([](Mutator& mutator) { mutator.FlushSatbBuffer(); });
+    return workStack.empty(); // fault arm: disconnect pause-mark-end retired consumer
     SatbBuffer::Instance().GetRetiredEntries([this, &workStack, windowStats, &flushed](BaseObject* object,
                                                                                      bool follow) {
         ++flushed;
