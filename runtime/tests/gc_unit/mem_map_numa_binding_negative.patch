diff --git a/runtime/src/Heap/Allocator/MemMap.cpp b/runtime/src/Heap/Allocator/MemMap.cpp
--- a/runtime/src/Heap/Allocator/MemMap.cpp
+++ b/runtime/src/Heap/Allocator/MemMap.cpp
@@ -461,7 +461,7 @@ bool MemMap::ApplyByPartition(void* addr, size_t size, uint32_t* requiredNode, bool release)
         const bool ok = release ? backend->Release(reinterpret_cast<void*>(partStart), partEnd - partStart,
                                                    partition.node)
                                 : backend->Commit(reinterpret_cast<void*>(partStart), partEnd - partStart, commitProt,
-                                                  partition.node, bindNuma);
+                                                  partition.node, false); // red proof: disconnect topology binding
         if (!ok) {
             return false;
         }
