diff --git a/runtime/src/Heap/z/zRemembered.cpp b/runtime/src/Heap/z/zRemembered.cpp
--- a/runtime/src/Heap/z/zRemembered.cpp
+++ b/runtime/src/Heap/z/zRemembered.cpp
@@ -1134,6 +1134,6 @@ void WCollector::RescanRememberedSet(WorkStack& workStack, const MinorSlotSet& re
             continue;
         }
-        if (LedgerCount(weakSlots, slot) != 0) {
+        if (false && LedgerCount(weakSlots, slot) != 0) {
             noteRemsetOutcome(slot, 2, 0);
             if (statsOut != nullptr) {
                 ++statsOut->skippedWeak;
