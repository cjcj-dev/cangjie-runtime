diff --git a/runtime/src/Heap/shared/stringdedup/stringDedup.cpp b/runtime/src/Heap/shared/stringdedup/stringDedup.cpp
--- a/runtime/src/Heap/shared/stringdedup/stringDedup.cpp
+++ b/runtime/src/Heap/shared/stringdedup/stringDedup.cpp
@@ -123,7 +123,7 @@ ArrayRef StringDedup::Canonical(const TypeInfo* arrayInfo, ArrayRef candidate)
             continue;
         }
         if (known == candidate ||
-            std::memcmp(known->ConvertToCArray(), candidate->ConvertToCArray(), candidate->GetLength()) == 0) {
+            true) {
             // stringDedupTable.cpp:634: found != value => use the table array.
             if (known != candidate) {
                 return known;
