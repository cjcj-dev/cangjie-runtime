# 708 返工 r5740859633
- 打回：DIFF 未记 runner sha256/三路径；无 bash -n 干跑；ops 工作树脏拷。
- 修：仓内 kkk2_diff 写 managed_runner.{sha256,path,cangjie_home,h48_rt,stained_rt}；campaign ops 还原三臂 HEAD，差异只写报告待主控落。
- 干跑：bash -n rc=0；缓存 sha 24dad0ae0 vs f72d29f9 命中缓存写出身份字段 dry_rc=0。

# 708 返工（前轮）
- 打回：kkk2_diff 对每个 sha 跑该树 kkk2_managed.sh ⇒ 基线旧单臂 JSON failed 恒空 ⇒ CAND-ONLY 假独红。
- 修：从候选 CS 抽出 kkk2_managed.sh 钉到 kkk2:/root/diff_harness/，两侧都跑这份；旧 JSON 无 arms 当缺失并补跑。
#712 implement notes
ZGC zHeap.cpp:253-257 alloc_page: page!=nullptr => _page_table.insert(page)
Ours zHeap.cpp:513-521 had get!=page skip. Removed.
Fixtures: unpublished InitRegion/TakeRegion must insert at Heap layer or RetireDescriptor.
PublishAllocatedPage -> Heap::alloc_page(ZPage*); stall uses Heap::alloc_page(size) with product default eden.

# 710 工作笔记（随提交）

任务：删 RegionList 家族。候选选择=页表迭代（已在 zGeneration.cpp:1407 select_relocation_set），
used/census=ZPageAllocator 计数器（usedPerGeneration/pageAllocatorUsed 已在）。

## 形态判定的关键事实（实测）
- 页身份谓词（IsFromRegion/IsGarbageRegion/IsThreadLocalRegion/IsPinnedRegion/IsUnmovableFromRegion/
  IsLoneFromRegion/IsFreeRegion/OnNamedList）= 链表成员身份本身（zPage.hpp:648-675, zPage.cpp:54）。
- 迁移策略：先入 ZPageRole 身份字段（RegionList 内部维护，语义不动），谓词改读 role，
  再把所有「走表」改成页表迭代（ZGC 形态 zPageTable.hpp:57-77），最后删表与 prev/next/owner 字段。
- ZGC 锚：zPageTable.hpp:60-100 迭代器；zGeneration.cpp:205-270 select；zPageAllocator.cpp:1307-1390 计数器；
  zRelocationSetSelector.hpp/cpp register_*。
- 基础设施差异（需记表）：Cangjie raw-pointer pin（Future/Mutex）ZGC 无 ⇒ pinned/rawpointer 角色保留为
  页状态，候选选择按 GetRawPointerObjectCount()>0 / pinned 角色过滤。
- 测试：删 test_region_list.cpp；BeginForwardingArena 改 selector.add_selected_small + install；
  used_generation 命名对齐 zPageAllocator.cpp:1375-1390。
