# 1107 fixture page lookup

坐标：开工回读 `c706c5ea0f4411fb12328f2b8a3164f7b5787cbc` rc=0。探索表基于 `4c4cbf53`，本 sha 上 8 处赋值与 5 个函数行号未漂。

## ZGC 锚

- 查表：`zHeap.inline.hpp:60` `ZHeap::page` → `_page_table.get`。我方 `zHeap.cpp:446` `Heap::page`，夹具 `gc_heap_fixture.hpp` `region0()`/`region1()`。
- 发布后才能查：`zHeap.cpp:253-257`。构造在 `PublishAllocatedPage`（`zunittest.hpp:48-51` → `Heap::alloc_page`）之前用局部 `ZPage*`。
- 非原地释放：`zRelocate.cpp:1040-1047` / 我方 `zRelocate.cpp:449-450` `Heap::free_page` 先摘表（`zHeap.cpp:498-502`）。
- 转发按地址：`zForwardingTable.inline.hpp:43` `get(zaddress)`。五个函数在 `relocate` 前保存 start 与 generation，之后 `generation_forwarding_table(gen).get(start)`。
- 提升换表：`zGeneration.cpp:941-948` / 我方 `zGeneration.cpp:1103-1108` `page_table().replace`。8 处 `fx.region0 = …` 是换表之后的缓存同步，删除。
- `SelectionCycleFixture` 页来自 `Heap::alloc_page`，访问器按 `starts[0/1]` 查，不按 `heapStart`。
- `LargeArrayFixture`（`test_followedge.cpp`）不改。
- `CheckNativeRoot` 的 `clone_for_promotion` 留在 relocate 之后：`flip_promote` → `ZPageTable::replace`（`zPageTable.cpp:61`）要求 from-page 仍是表项，故该路径不是 `free_page`。原地保留见 `zRelocate.cpp:438-448`。

## 不改

不改 `runtime/src`。不改断言。不改 `test_followedge.cpp`。
