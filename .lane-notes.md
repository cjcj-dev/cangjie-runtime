#716 implement notes

ZGC: zReferenceProcessor.cpp:450-452 process_references always `_workers->run`.
ZGC: zRemembered.cpp:565 scan_and_follow always `young()->workers()->run`.
ZGC: ZReferenceProcessor constructed with old workers (zGeneration).

Ours: ReferenceProcessor lives on FinalizerProcessor, workers default nullptr.
MarkComplete ProcessReferences → work() on driver → WorkerThread::worker_id()==UINT32_MAX → CHECK zReferenceProcessor.cpp:52.

Fix: rebind set_workers at old mark_start; process_references rebind from old().Workers(); remset scan no coordinator fallback.
