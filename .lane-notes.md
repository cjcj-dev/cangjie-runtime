# 707 implement notes

ZGC zPage.cpp:64-72 clone_for_promotion = new ZPage(old), copy type/vmem/top, no replace.
ZGC zGeneration.cpp:941-948 flip_promote = page_table.replace + promote_used + increase_freed/promoted.
ZGC zRelocate.cpp:1340-1363 flip_age_pages: clone vs reset(toAge); promotion calls flip_promote; register_flip_promoted(from pages).
ZGC zPageTable.cpp:67-76 replace DOES register_with_remset if new is old (task text was wrong).
ZGC zRelocationSet.cpp:182-202 destroy_and_clear via safe_destroy_page (descriptor only).

Deleted: PromotionPage, CloneForPromotion, GetZPage, GetGhostFromRegionAt, InitRegionAt, regionInfoStart slot, flipPromotedPages unique_ptr vector.
Lookups: Heap::page / ZPageTable::get.
