# 1079 notes

坐标基于 ed49f199032ba24000f11f5aa6a2ea226920fb4d。

ZGC zRelocate.cpp:1012-1047 do_forwarding：in_place 保留页，否则 detach 后 free_page。
我方 zRelocate.cpp:433-451 同一分路。不改产品，除非新断言证明非原地且抽样时槽仍是选择时的 From 页。

用例 test_zPage.cpp CopiedSourceLeavesPageTable：按 forwarding->in_place() 分类。
原地：Heap::page 非空且 GetRegionStart 等于选择时起点（晋升 clone 换描述符，起点不变，zGeneration.cpp:1114-1117）。
复制：槽空或另一页，且当前 occupant 不是 From（zGeneration.cpp:1205 置 From，复制臂 free 前不改角色）。
禁止 retired==published 或 retired==2。
