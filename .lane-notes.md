# 1104 notes

主控裁决 C（outbox 20260924T213919Z）：不要前置 on_safepoint，也不要延后 completed。
relocate-start 出口的帧处理必须用 flip 前保存的颜色走 relocate_or_remap，把 cset 槽写成 to 地址。

ZGC 锚：
- zGeneration.cpp:432-452 skip_thread_oop_barriers，直接 before_zoperation
- zStackWatermark.cpp:95-99,177-181 在发布新 epoch 前保存上一颜色
- zUncoloredRoot.inline.hpp:62-68 颜色非 load-good ⇒ relocate_or_remap 再写回
- zGeneration.inline.hpp:131-139 cset 内对象当场 relocate_object
- stackWatermark.cpp:205-218 深栈懒处理；我方无 return statepoint，on_safepoint 一次走完并置 done（zStackWatermark.cpp:37-48,185）

我方：
- 已撤 VM_ZOperation::pause 里 BeforeZOperation 前的全线程 on_safepoint
- save_old_watermark 在 TryBegin 之前读颜色；线程掩码已是 flip 后 store-good 时用上一 epoch 位，避免 is_load_good 跳过 remap
