# 1085 返工笔记

打回：偏移早退行为成立，强度解析与 referent 判定仍合层。

| 层 | ZGC | 我方 |
|---|---|---|
| 模板只路由 known/unknown | gc/shared/accessBarrierSupport.inline.hpp:31-35 | runtime/src/Heap/z/accessBarrierSupport.inline.hpp |
| 运行期解析消费 bool，再决定 DecoratorSet | gc/shared/accessBarrierSupport.cpp:29-45 | runtime/src/Heap/z/accessBarrierSupport.cpp |
| 谓词先偏移再读类型，返回 bool | classfile/javaClasses.cpp:3954-3968 | runtime/src/Common/BaseObject.cpp is_referent_field |
| 消费者 | gc/z/zBarrierSet.inline.hpp:211、216 | runtime/src/Heap/z/zBarrierSet.inline.hpp is_store_barrier_no_keep_alive / load_barrier_on_unknown_oop_ref |

适配：Cangjie 弱引用只有 TypeInfo::IsWeakRefType（TYPE_KIND_WEAKREF_CLASS，MClass.inline.h:99），没有 REF_FINAL/REF_PHANTOM。referent 槽是 TYPEINFO_PTR_SIZE。无头记录不能在偏移命中前当实例读 klass，沿用既有非对象拒绝（nullptr / 非堆）再读类型。
