# 754 notes

坐标基于 `4c4cbf53b44497103e76e2a47a8fa35f5d7a7287`。

ZGC：
- oopStorage.cpp:774-784 `release` 不写槽；`check_release_entry` 要求 `contains_null`
- zCollectedHeap.cpp:334-336 `contains_null` = `is_null_any`
- oopHandle.inline.hpp:63-67 / weakHandle.cpp:48-54 / jniHandles.cpp:149-162：先 `NativeAccess<>::oop_store(nullptr)` 再 `release`
- zBarrierSet.inline.hpp:258-265 `oop_store_not_in_heap` 写 `store_good`，null 也不是裸 0
- zAddress.inline.hpp:700-702 `is_marked_finalizable` 要求非裸 null。不许删、不许放宽
- zBarrier.inline.hpp:338 与 :356 `assert(!is_null(good_ptr))`，查的是 heal，不是 old

我方：
- OopStorage.cpp `Release` 删掉 `StoreColoured(zpointer::null)`，前置 `is_null_any`
- zReferenceProcessor.cpp 四个 `Release` 前补 `NativeAccess<>::oop_store(nullptr)`
- WeakHandle.cpp 与 RemoveExportRoot 已经先 `oop_store(nullptr)`，Release 不再覆盖
- zBarrier.inline.hpp `barrier` 与 `remap_young_relocated` 补 heal 非裸 null 断言
