# 1102 rework

Review r5820131934 rejected the promotion null→color_null path and the store-good remset patches. Those invert ZGC zBarrier.inline.hpp:668-678 and zRelocate.cpp:747-749 / :1230-1232.

ZGC zStoreBarrierBuffer.cpp:173-182: a buffer entry flushed because young mark has started calls scan_remembered_field (zGeneration.inline.hpp:162-164), which both marks the current young referent and remembers the slot. The previous call only ran RemsetBarrierOnOopField, so the slot never entered the next young mark's remembered set. The young page could then be freed while the old field still named it, and MarkFromOldSlowPath dereferenced a null page.

Promote barrier is back to the single store-good barrier. Store-good consumers return without adding a remset entry.
