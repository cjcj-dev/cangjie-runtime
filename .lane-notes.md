#716 implement notes

ZGC process_references: zReferenceProcessor.cpp:443-458 always `_workers->run(&task)`.
ZGC construction: zGeneration.cpp:983 `_reference_processor(&_workers)` on old generation.
ZGC remset: zRemembered.cpp:565 `young()->workers()->run`.

Ours: ReferenceProcessor lives on FinalizerProcessor (not generation). workers bound at
zCollectedHeap.cpp:126 and ZGenerationOld::mark_start. process_references rebinds from
old().Workers() then always run; no coordinator work() fallback.

CHECK worker_index: zReferenceProcessor.cpp:49-54 WorkerThread::worker_id TLS.
Producer: workerThread.cpp:58-59 set_worker_id in dispatcher.
Consumer: worker_index during discover/work.

Infrastructure: processor not owned by ZGenerationOld; set_workers instead of ctor const.
