#712 implement notes (from main)
ZGC zHeap.cpp:253-257 alloc_page: page!=nullptr => _page_table.insert(page)

#716
ZGC zReferenceProcessor.cpp:443-458 process_references: only `_workers->run(&task)`.
ZGC zGeneration.cpp:983 `_reference_processor(&_workers)` at old generation construction.
Ours: inject once when old ZWorkers is created (InitializeWorkers). No mark_start set_workers, no else work().
