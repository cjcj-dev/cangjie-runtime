# 1090 notes

ZGC:
- zRelocationSet.hpp:70-78 `ZRelocationSetIteratorImpl` inherits `ZArrayIteratorImpl<ZForwarding*, Parallel>`
- zRelocationSet.inline.hpp:35-41 ctors pass `_forwardings` / `_nforwardings` (or nullptr, 0)
- zArray.inline.hpp:131-132 parallel `next` is `_next.fetch_then_add(1u, memory_order_relaxed)`
- zRelocate.cpp ForwardTask::work is the consumer of `ZRelocationSetParallelIterator::next`

Ours before the change:
- zRelocationSet.hpp iterator kept its own `_i` and did `_forwardings[_i++]` for both serial and parallel
- zArray.inline.hpp:128-138 already has `next_parallel` via `_next.fetch_add`
- ForwardTask::work (zRelocate.cpp) calls `iter.next`; claim is a separate CAS

The parallel template parameter was unused. Null slots are not skipped; install writes every index.
