runtime/tests/gc_unit/build_standalone/
runtime/target/
*.tar.gz
# Python analyzer bytecode (housekeeping only).
__pycache__/
*.pyc

# lane working notes live outside the repository (agent_scratch)
.lane-notes.md
