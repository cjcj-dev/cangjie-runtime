# B17 managed observations on the #585 toolchain

Runtime coordinate: `fd4a816c5e098b821ec2f1d8f365233ad7148747`.
Toolchain: `gate-colored-23e45a2e`, LLVM `23e45a2e9dfbfc4a708d99bc7895fbc1d8ffcbaf`.
This is an observation delivery, with no runtime or CHECK changes.

Each runner ran three times using the testable SO built from that runtime
coordinate. Every runner returned 1:

| Runner | Target result | LOADFC log records per sample | MARKSTALE log records per sample |
| --- | --- | --- | --- |
| finalizer_trigger | rc=0, done=1, finalized=0 (expected 64) | 0 | 10 |
| segmented_array_managed | full rc=134; young not reached | 1 | 0 |
| phase_entry_trigger | minor/major/timer rc=0; minor/major guard rc=1 | 0 | 0 |

MARKSTALE is sampled at powers of two. Ten records end at `n=512`; this
is not an exact count of all stale targets. The initial phase runs lacked
`tools/zstat_pillars.py` because the two-configuration build archive contains
only runtime. After extracting the same commit's tools, three separate phase
runs passed the analyzer self-tests but still failed the actual log guards:
minor `no rec=cycle records`, major `major_explicit_shape=major`.

The existing single-breakpoint `b17_capture.py` stopped three times at
`WCollector::ForwardUpdateRawRef.unresolved`; first-stop target/holder/slot
fields and actual mappings are preserved. Later legacy type lookups in that
script report errors; they do not support further conclusions.

Default/filler/testable standalone results: 0/0/0 (526/526/708 tests).
Filler reused the default ELF with `CJRT_HEAP_FILLER=0`.
OHOS attempt: 21, missing `MRT_GC_UNIT_OHOS_HOST_RECEIPT`.
The real managed gate returned 1 at the finalizer assertion. `gate_all.py`
with the named toolchain returned 1 at unit ruler snapshot mismatch, before
its managed arm ran. Neither result establishes acceptance.

Evidence root on kkk2:
`/root/sym_cangjie_runtime_593_implement_r5670415547/`.
`evidence/observations.json` indexes individual rc, ELF hashes, loader/maps,
and log observations. `evidence/runtime-identity.json` gives SO hashes and
source stamps; `evidence/585-identity-verified.json` verifies all 128 previous
identity entries. `evidence/old-control.json` indexes the permitted #564
historical positive control; it is not a same-SO causal comparison.

`tools/run_b17_593.py ROOT CPU1 CPU2 CPU3` invokes the existing runners and
records observations. `B17_RUNNERS` selects names and `B17_OUT` selects an
independent output family. Supply all runtime and tools files before running;
use an unused output family and reserve the CPU ranges first.
