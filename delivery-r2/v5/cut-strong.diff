--- a/runtime/src/Heap/z/zLiveMap.inline.hpp
+++ b/runtime/src/Heap/z/zLiveMap.inline.hpp
@@ -54,11 +54,11 @@
             const uint64_t marked = old | startPair;
             if (marked == old) {
                 incLive = false;
-                return false;
+                return true;
             }
             if (word.compare_exchange_strong(old, marked)) {
                 incLive = (old & maskInfo.liveStartBitMask) == 0;
-                return true;
+                return false;
             }
         }
     }
