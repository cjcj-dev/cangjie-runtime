diff --git a/runtime/src/Heap/z/zObjectAllocator.cpp b/runtime/src/Heap/z/zObjectAllocator.cpp
index e2bf238d0..77492d073 100644
--- a/runtime/src/Heap/z/zObjectAllocator.cpp
+++ b/runtime/src/Heap/z/zObjectAllocator.cpp
@@ -266,7 +266,7 @@ void RegionManager::RetireSharedPages(PageAgeRange ages)
 {
     for (PageAge age : ages) {
         for (size_t cpu = 0; cpu < SharedPageCPUCount(); ++cpu) {
-            objectAllocators[untype(age)]->smallPages[cpu].page.store(nullptr, std::memory_order_release);
+            // Control arm: leave the shared allocation page installed.
         }
     }
 }
