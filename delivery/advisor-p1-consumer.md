LANE=sym_cangjie_runtime_606_implement_r5673376405
ROLE=implement
跟进A裁定：consumer relocatable断言真实集预演命中现有入口，不能直接启用。
候选879b39265，kkk2:/root/sym_cangjie_runtime_606_implement_r5673376405-green/unit-testable.log。rc=1；例MarkPort203Entries.SerialCollectionConsumesArrayTails、StripedCollectionConsumesArrayTails、FinalizableArrayClosureAccountsWithoutStrongUpgrade等在新增MarkEntryObject的IsRelocatable断言命中；YoungConc.Y2yDirtyVisibleBeforePauseMarkEnd同样命中。另有产品真实集尚未分类，不能拿这些全当负向对照。
原因边界：按已裁A保留未获证裸根/字段生产入口；它们仍直接构造MarkStackEntry，缺少typed入口allocating早退。P1若保留新增consumer强断言，既有产品调用集不能通过；若consumer加allocating return则分路点与ZGC不同；若强转这些裸入口则违反A。
建议A2：P1 typed入口与统一consumer内部处理/竞争语义照Z，暂不启用新增relocatable CHECK（按fail-closed上线真实集命中正常输入须先修生产端的合同），记录P2/P3迁入时启用；不删除任何基线CHECK、不加宏或豁免。consumer仍处理旧入口，不把过渡形态标✅。
B：P1必须等待P2/P3资格交付并连同生产端迁移后才能交；若B请协调依赖循环P2依赖P1。
请裁唯一动作；本棒继续页/返回约定测试修正及真实入口构造，WIP。
