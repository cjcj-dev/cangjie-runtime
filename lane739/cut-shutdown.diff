diff --git a/runtime/src/Heap/z/zDriver.cpp b/runtime/src/Heap/z/zDriver.cpp
index db448dd05..13a3ea03d 100644
--- a/runtime/src/Heap/z/zDriver.cpp
+++ b/runtime/src/Heap/z/zDriver.cpp
@@ -71,7 +71,7 @@ void ZDriver::run_thread()
     // Runtime finalization is a host lifecycle extension. Every driver exit,
     // including an abortpoint, answers queued allocations before terminating.
     struct FinishStalls {
-        ~FinishStalls() { Heap::GetHeap().page_allocator().StopStalledAllocations(); }
+        ~FinishStalls() {}
     } finishStalls;
     for (;;) {
         const ZDriverRequest request = port.receive();
