diff --git a/runtime/src/Heap/z/zPageAllocator.cpp b/runtime/src/Heap/z/zPageAllocator.cpp
index debb68628..429af6fc3 100644
--- a/runtime/src/Heap/z/zPageAllocator.cpp
+++ b/runtime/src/Heap/z/zPageAllocator.cpp
@@ -767,7 +767,7 @@ void RegionManager::NotifyOutOfMemory()
     while (ZPageAllocation* request = stalled.first()) {
         if (!HasAllocSeenOld(request)) { return; }
         stalled.remove(request);
-        request->Satisfy(false);
+        // controlled fault: terminal failure notification omitted
 #if defined(MRT_ALLOCATION_STALL_OBSERVE)
         ++stallDequeued;
         ++stallFailed;
