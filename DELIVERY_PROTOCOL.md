# 统一交付协议 v2

> **一句话**：每一条主张都必须携带「它是怎么被验证的」。
>
> 对五种角色全部适用（explore / synthesize / implement / review / merge）。
> 可机器判定：`cjops deliver check --lane <棒名>` ⇒ **退出非 0 的交付不许收割**。
>
> v1（557 行，按时间堆叠）已归档 `ops/archive/DELIVERY_PROTOCOL-v1-through-20260824.md`。
> v2 是**整体重写**：同样的要求，按**它们回答的问题**归类，⛔ 不再按「哪条棒触发了它」排列。

---

## 本协议的骨架

所有要求都在回答四个**递进**的问题。上一关不过，下一关无意义：

| 关 | 问题 | 防的失败态 | 节 |
|---|---|---|---|
| **0** | 你说的是什么？ | 主张没有可复读的形式 | §1 |
| **1** | 你测的是**产品**吗？ | 测试里另编了一套被测组件 | §2 |
| **2** | 你测到了**全部**吗？ | 真的臂 + 漏掉的承重点 | §3 |
| **3** | 这份**证据本身**可信吗？ | 臂跑在别的构型上 / 哈希绑不回文件 | §4 |

⚠ 三关是**递进**的：§2 过了才谈 §3，§3 过了才谈 §4。
⇒ 审查按这个顺序查；**打回时要说清卡在第几关**。

---

# §1 · 报文形式

## 1.1 报告头 —— 前五行**位置固定**

```
第1行  PROGRESS=<WIP|TRIAGED|DONE> · verdict=<一句话判词> · LANE=<棒名>
第2行  DELIVERY_REF=<仓>|<分支>|<40位sha>      或 DELIVERY_REF=none|no-code|<原因>
第3行  SIDE_EFFECT: <一句话>                    无副作用就写「无」
第4行  ROLE=<explore|synthesize|implement|review|merge>
第5行  EVIDENCE=<绝对路径,逗号分隔>              或 EVIDENCE=none
```

位置固定是有意的 —— 这样「没写」与「写错地方」**都能被判出来**。

**三态**：

| 首行 | 含义 | 派发器 |
|---|---|---|
| `WIP` | **我还在做** | **续跑** |
| `TRIAGED` | 停下等裁决 | 停 |
| `DONE` | 做完了 | 停 |

⚠ 还在做就写 `WIP`。写 `TRIAGED` 曾害一条棒空停 6 小时。
⚠⚠ **提问之后仍要保持 `WIP`** —— `merge_runtime5` 在答复落盘前 4 分钟转了终态，派发器随即停它，**那条指令永远没送到**。

**`DELIVERY_REF` 只认两种形态**：

```
有代码：<仓>|<分支>|<40位sha>       ⇒ 必须是真实存在的 40 位 sha
无代码：none|no-code|<一句话原因>    ⇒ 例：none|no-code|对齐轮，只读
```
自由文本一律判违规 —— 「那个分支上」「见下文」都不行。

**`EVIDENCE` 必须是绝对路径** —— 相对路径不可复读（`cd` 之后读错文件本战役已犯 5 次）。

## 1.2 CLAIM 块 —— 每一条实质主张

```
CLAIM: <你的断言，一句话>
  METHOD: <read|measure|test|control-arm>
  EVIDENCE: <file:line | 绝对路径 | 可复跑的命令>
  N: <样本数>          METHOD=measure / control-arm 时必填
```

| METHOD | 什么时候用 | EVIDENCE 该是什么 |
|---|---|---|
| `read` | 读源码得出的结论 | **`file:line`**（不是「我看过了」） |
| `measure` | 跑出来的数 | 结果文件绝对路径 + `N:` |
| `test` | 由测试证明 | 测试名 + **故意破坏转红的证据** |
| `control-arm` | 有对照臂的比较 | 两臂各自路径 + `N:` |

**三条硬规矩**：

1. **`measure` / `control-arm` 必须给 N**。主控一天内三次从 N=1~2 断言确定性，三次都被下一发推翻。
2. **任何「没有 / 为 0 / 全绿」的主张，必须配一条阳性对照 CLAIM**。
   - 恒 0 是高危信号；「恒非 0 且很大」同样要对照（尺可能已腐烂）。
   - **读数之前先确认那个程序真的运行了** —— 看 rc，别只看 stdout。
   - 同理：**先验尺子**。一条检索返回 0 时，先问「这把尺子会不会动」再下结论。
3. **凡涉及负载的数，必须带 ELF sha256 + SO 血缘 stamp + 核域 + 两端 load average**。
   离开这些，那个数无法解读。

## 1.3 `## FALSIFIED` 节 —— **必须存在**

```markdown
## FALSIFIED
任务书里被你证伪的前提，逐条列出（带证据）。没有就写「无」。
```

⇒ **推翻前提是加分**。主控的「疑似缺」被棒实核 12 次全错，方向一致：一律低估我方已有的。
⇒ **自我更正也写这里**，并且**不作为打回理由** —— 自己发现并更正，与被审查抓到，是两回事。

---

# §2 · 第一关：你测的是**产品**吗

## 2.1 `## 产品接线证明`（implement + DONE **必须有**）

```markdown
## 产品接线证明
| 测试名 | 它调的**产品**函数(file:line) | 断开这条接线，测试会红吗 | 凭什么这么说 |
|---|---|---|---|
```

**三条自查**（任一为「是」⇒ 你的测试没在测产品）：

1. 我的测试有没有**自己重编/自己拼**了被测组件的一份副本？
2. 我把中间值**手工喂**给下游了吗？（而不是让产品代码算出来）
3. 如果把产品里那个调用**整行删掉**，我的测试还会绿吗？

## 2.2 断线臂 —— **必须交实际产物，光有表不算**

⛔ 一份**专门为修这条派的返工件**，任务书里逐字写了自查三问，它**仍然**交出「产品接线断开仍全绿」的测试。
⇒ **表能靠推理填，日志不能。**

节里**必须出现证据文件的绝对路径**（校验器查这个）：

```
① 改产品源码，把测试依赖的那条产品调用断掉（删掉/换成空实现）⇒ 存 cut.diff
② 重编产品 SO（不是只重编测试）⇒ 存构建 log
③ 复跑同一组测试 ⇒ 存 cut-*.log 与 rc ⇒ 必须 rc≠0（红）
   若仍 rc=0 ⇒ 你的测试没在测产品 ⇒ 回去改测试
④ 恢复源码，复跑 ⇒ 存 restored-*.log（应回绿）
```

## 2.3 断在**承重点**，不是最容易变红的那个点

⛔ 三条审查同时发现同一件事：三份交付**都有红臂**，三份**都断在不承重的位置**。
⇒ **交了红臂 ≠ 断对了地方。**

⇒ 节里必须给出**产品树的 `file:line`**（`runtime/…` / `packages/…`，不在 `tests/` 下），并回答：
> **产品在运行时是从哪个调用点走到这条路的？我断的是不是那一个？**

⚠ 自查：若你断的那处**只有测试会走到**，它不是承重点，重新选。

## 2.4 跨文件的臂：「断」是**瞬态**的，不构成所有权主张

承重点常落在邻棒的文件里（因为要向上走到运行时入口，见 §3.2）。
⇒ **允许对邻棒文件做瞬态受控破坏取证** —— 条件是**最终零 diff**：

```
① 用哈希证明零 diff，不是「我恢复了」
   ⇒ 破坏前后各记一次 sha256sum <文件> ⇒ 两个哈希必须相同，两次输出都贴
   ⇒ 再加 git status --porcelain -- <文件> ⇒ 必须空
② 只在隔离树里做 ⇒ 别在共享树 / 别人的证据树里破坏
③ 报告里写明：动过谁的文件、属谁的域、最终零 diff（附前后哈希）
   ⇒ 合并 agent 需要这句 —— 它按内容核时才不会把这处当异常
```

## 2.5 三种**合法的例外**

⚠ 先说这道门**真正要防的**：一份单测**在测试 ELF 内另编了一套被测组件** ⇒ 测的是副本。
⇒ **要害是「测试用的是另一份拷贝」，不是「有没有重编 SO」。**

### 例外一 · 当前还没有产品消费者的新 utility

```
断不出承重点是正常的 ⇒ 如实写：
  「当前基线产品消费者 = 0（附检索命令与输出）⇒ 无承重点可断」
  「首个消费者是 <X>，有意后置 ⇒ 产品接线证明推迟到它迁入时补」
⛔ 别为了让门过而断一个假的接线点
⛔ 也别新增测试专用导出去凑一个臂 —— 那只证明测试包装，不证明真实消费者
```

### 例外二 · header-only 模板

```
模板在测试 ELF 里实例化 ⇒ 不从重编后的产品 SO 绑定 ⇒ 「重编 SO 看红」结构上不成立
但 header-only 按构造只有一份真值 ⇒ 「测的是副本」那个失败态不可能发生
改交这两样：
  (a) 在头文件本体上故障注入 ⇒ 复跑 rc≠0 且只红该项 ⇒ 恢复回绿
  (b) 证明不存在第二份拷贝（检索命令与输出贴出来）—— 这一条替代了断 SO 的作用
```

### 例外三 · 唯一承重点

```
若 N 个面在结构上必经同一个出口 ⇒ 断那一刀就是这 N 面的证明
⇒ 不要求做 N 份彼此独立的断线臂（那是无谓劳动）
但要给三样：
  ① 唯一性的源码证明 ⇒ 逐面给 file:line，说明它为何绕不过该出口
  ② 至少一份单面臂 ⇒ 挑最可能绕过的那一面单独断 ⇒ 只红该面对应项
  ③ 反向对照 ⇒ 那一刀红的项必须全部属于该机制（带红无关项 = 过度捕获）
```
⚠ **验收标准跟着【结构】走，不跟着【条目数】走。**

### 三种例外共同的要求

**必须写明缺口**，不许让读者以为已经证过：
```
例外一/二 ⇒ 「产品路径确实走到它」本轮未证 ⇒ 推迟到首个消费者
⇒ 推迟 ≠ 豁免 —— 首个消费者那条棒必须补上产品 SO 的接线证明
```

---

# §3 · 第二关：你测到了**全部**吗

## 3.1 `## 承重面清单`（implement + DONE **必须有**）

⛔ 五份交付**都有真的臂**，但五份都漏掉了别的承重接线（断开后仍全绿）。
⇒ **形状：实现者按【自己脑子里的模型】枚举消费者 —— 而那个模型正是产生缺口的东西。**
⇒ 所以枚举必须**机械**，不能靠回忆。

```markdown
## 承重面清单
检索命令：`git grep -n '<你改动的符号>' -- runtime/src`（命令与输出都贴出来）

| 消费者/分支 | 产品 file:line | 有无臂 | 臂的产物路径 | rc |
|---|---|---|---|---|
```

**三条硬规矩**：

1. 清单必须**由检索命令导出**，命令与输出都要贴 ⇒ 不许只给结论。
2. 常见的**分支轴**都要展开：full/range · 成功/失败 · pre/post · 静态/动态 · 单 worker/多 worker · 公共入口/内部入口。
3. 判断**不需要臂**的，要写理由 ⇒ **「我觉得等价」不算理由**。可接受：「它没有产品调用者（附检索输出）」。
4. **grep 命中 ≠ 语义命中** —— 逐条确认每个命中是**调用点**，还是**字符串 / 注释 / 同名的别处**。
   ⛔ 实付（主控自己）：grep 一个函数名去定位它的守卫，而命中的那一行是**另一个诊断的消息串里恰好含该函数名**
   ⇒ 「离命中最近的那条断言」落在了一处**同名但完全无关**的既有守卫上，差一点把它删掉。
   ⇒ 判据：**贴出每个命中所在的整行**，别只贴行号。
   ⚠ **反向同样成立：语义在场而 grep 漏判。** 实付：ON:ON 的产品 SO 里 hook 符号是
     **版本化**的（`SYMBOL@@VERSION`），而判据用了精确行尾匹配 ⇒ **符号在场却仍走 NOT_RUN 分支**
     ⇒ 那道门因此**仍是永久空洞**。
     ⇒ 凡是拿 `nm` / `grep` 判「符号在不在」的，**必须做双向对照**：
       有该符号的产物 ⇒ 判据必须为真；没有的产物 ⇒ 必须为假。缺任一侧都不算验过。

⚠ 审查角色会做同一件事 —— 它会挑一条**你没验的**改掉看红不红。
⇒ 这一节不是给主控看的，是**你自己先做一遍它要做的事**。

## 3.2 枚举要**向上走到运行时入口**

⛔ 5 份送审 5 份全被打回，同一形状：它们**都交了真的红臂、也都给了机械导出的清单**，但清单**深度不够** —— 一份交了 9 条臂 / 8 种调用形态，仍被打回，因为「消费者枚举在 `GCStackInfo` 停止，漏掉真实运行时的 `Mutator → StackManager → GCStackInfo` 根修复入口」。

⇒ **`git grep <符号>` 只给【直接调用者】—— 而承重点往往在【上面两三层】的运行时入口。**

清单要分四步做：
```
① 先 git grep 出直接消费者
② 从每个直接消费者【向上】走调用链，直到走到运行时真正发起的那个入口
   （mutator 操作 · GC 相位 · 线程入口 · 屏障发射点 · safepoint 动作）
③ 臂放在【入口】，不是放在直接调用处
   判据：「产品运行时是从【哪一行】开始走到这条路的？」
④ 每一层都贴检索命令与输出
```
⚠ 自查：若你的臂全都断在**同一个文件**里，那多半就是停在直接调用者了。

## 3.3 `## 测试增删`（implement + DONE **必须有**）

⛔ 一份交付把**唯一的跨回绕行为用例替换成了一个 ignored 用例** —— 上一版五项、本版仍是五项、总数仍是 318。
⇒ **一次覆盖面回退，被一个不变的计数掩盖住。**

```markdown
## 测试增删
相对基线 <sha> 的测试名**集合差**（不是计数差）：
- 新增：T1 · T2
- 删除：T3 —— 理由：<为什么可以删>
- 改名：T4 → T5
```

**两条硬规矩**：
1. 报**集合差**，不是计数 —— **「测试数不变」不等于「覆盖面不变」**。
2. **每一条删除或替换都要写理由**。可接受：「它已被 X 完全覆盖且 X 更严」。不可接受：「换了个更好的」「不再需要」—— 那要说清**原来那条保的是什么**。

⚠ **别为了让某条阻断消失而删测试** —— 那不是修好，那是把尺子折断。

---

# §4 · 第三关：这份**证据本身**可信吗

> 前两关问的是「你测对了地方吗」。这一关问的是「**你交上来的这份记录，能不能绑回那次实际运行**」。
> 三条规则，防三种不同的断裂。

## 4.1 构型：宏门控的产品形态

若你为了让测试调到产品实现，动了**产品形态**（导出 / 去内联 / `friend` / 加访问器）：

```
默认（产品编译）= 性能优先 ⇒ 保持内联 · 无导出 · 无 friend
定义测试宏时 ⇒ 才导出 / 去内联 / 开 friend
```

⚠ 必须是**真正的编译期开关**（影响是否生成导出符号），不是运行期 env ——
前科：93 个 `MRT_*` 的 getenv 被编译期换成 `nullptr` ⇒ 设 env 静默无效。

**判据**：问「关掉这个宏，产品还**正确**吗？」
- 仍正确 ⇒ 可以宏门控（它只是可测性）
- 不正确 ⇒ **它是正确性装置** ⇒ **不许门控**，必须默认生效

每条接线验证臂必须**标注构型**，并回答：**「这条臂的结论能否外推到产品构型？凭什么？」**
```
可接受：内联只改调用形态，不改「该调用点是否存在/是否被走到」⇒ 存在性可外推
不可接受：「应该一样」「逻辑相同所以没问题」
⛔ 性能结论一律不可外推 —— 那正是宏要隔开的东西
```
全文：`ops/design/TESTABILITY_MACRO_GATE.md`

### `## 双构型证明`（**只对宏门控的交付**）

⛔ 一份交付在宏后加了产品侧发射点，又加了一个**无条件**要求该发射点的门脚本 —— 而该宏默认关。
⇒ **任何人用默认配置建 runtime 都会失败，含发布构建。**
⇒ 而它**通过了对抗审查** —— 因为审查只建了**宏开**那一种构型。

凡是交付里出现 `#if defined(<宏>)` / `if (<宏>)` 的构建分支，报告必须有这一节，两行都要**真实构建的 rc**：

| 构型 | cmake 变量原样 | 构建 rc | 该项状态 | 产品 SO sha256 |
|---|---|---|---|---|
| 产品（宏关） | … | **必须 0** | PASS / NOT_RUN | … |
| 测试（宏开） | … | 0 | **必须 PASS** | … |

**两条硬判据，必须同时兑现**：
1. 宏**关**的构型 ⇒ **构建必须成功**（rc=0）。用户令：真实编译时性能优先 ⇒ 产品构型不欠测试任何东西。
2. 该检查**必须在某处真的跑过并能失败**。
   ⛔ 「宏关时跳过」单独成立 = **永久空洞** ⇒ 那正是「绿门只证明没有守卫响」。

⚠ **审查角色照此加一问**：**「你建过宏【关】的那一种吗？」** 只建一种 ⇒ **B 类打回**（补建一次即可，不必重做实验）。

## 4.2 同构型：三臂必须是**同一个**构型

⛔ 一份交付报了绿臂 333/333、断线臂 331/341、恢复臂 341/341 —— 那个 333 的测试二进制**建于最后一次源码改动之前**，且缺 8 个由开关收入的测试。
⇒ **三臂不在同一个构型上 ⇒ 对比无意义 ⇒ 「臂不成立」。**

凡是给「绿 / 断线 / 恢复」三臂的，每臂必须记**测试 ELF sha256 + 产品 SO sha256 + 构建时刻**。

**四条机械判据**：
```
① 三臂里【只有一个产物变】，且必须是【承载那一刀的】那个
   ⇒ 刀在产品 SO ⇒ 只有 SO 变；刀在测试侧/头本体 ⇒ 只有测试 ELF 变
② 承载刀的那个产物：绿 = 恢复，断线 ≠ 它们
   ⇒ 绿≠恢复 ⇒ 要么没真恢复、要么绿臂不是最终构型 ⇒ 皆为打回
③ 其余产物：三臂逐字节相同
   ⇒ 这一条最强：它证伪了「测试 ELF 里另编了一套被测组件」——
     若真另编了一套，断线就会让 ELF 也变；它没变 = 好消息
④ 三臂总项数必须相等 ⇒ 不等 = 不同构型 ⇒ 必须逐项交代差值来源
   ⚠ 「我新增了几个测试」不能解释掉 —— 新增之后，三臂都要重取
```
⚠ 每臂构建时刻**必须晚于**最后一次源码改动时刻，两个时刻都贴出来。

⛔ **本条初版曾要求「断线臂的测试二进制 sha 必须不同」，被棒按结构反驳并作废** ——
当刀切在产品 SO、测试又动态链接它时，测试 ELF 必然逐字节相同。
⇒ 教训：**立判据时自问「换一种合法结构，这条还成立吗？」**

## 4.3 产物哈希必须在**产生的那一刻**捕获

⛔ 一份交付的断线臂 post-build 门以 rc=2 退出，**gmake 随即删掉刚链接的 SO**；交付方为了留哈希**事后重执行了一次链接**。
审查做复现实验：链接器本身逐字节确定，**但两次都没复现历史的那个哈希** —— 因为历史那次的对象集合已随构建消失。
⇒ **那个哈希绑不到测试真正加载的那个文件。**

⚠ 审查的措辞值得照抄：这**不证明**它是错的，只证明**当前交付无法验证**。⇒ 判 **B 类**，不是 A 类。

凡是拿**产物哈希**当判据的，哈希必须在产物存在的那一刻取。三档，按强度：
```
①   链接完成后立刻 sha256sum，在任何可能删它的步骤之前
②   让测试跑在一份被保留的副本上（LD_LIBRARY_PATH 指向它）
     ⇒ 「被哈希的文件」与「被加载的文件」是同一个 ⇒ 身份闭合
③   测试进程运行期间读 /proc/<pid>/maps 拿实际加载路径，就地哈希 ⇒ 无可辩驳
⛔ 不许事后重建 —— 哪怕链接器确定，你也无法证明输入集合与当时相同
```
⚠ **三臂必须用同一种办法**（别绿臂用②、断线臂用①）。

**一般形**：**「可复现」不等于「可事后重建」** —— 前者要求算法确定，后者还要求**输入仍在**，而构建过程会吃掉输入。

## 4.4 两臂比较：先确认**配方相同**

⛔ 一份交付报「宏门控集合差 311/390」—— 而默认臂走 `build.py build`、测试臂走直接 `cmake --build`，**两臂配方不同**。同配方重做后是 **44 / 0**（测试构型是产品构型的严格超集，正是该有的形状）。

⇒ 任何两臂比较，报告里**必须写明两臂走的是哪一条构建入口**，且必须是**同一条**。
⇒ 比较两臂之前，**先让同一臂比自己**（噪声底）。噪声底高于你要测的差异 ⇒ 那个判据当场作废，换判据。

---

# §5 · 角色专属要求

| 角色 | 额外要求 |
|---|---|
| **explore** | DELIVERY_REF 应为 `none\|no-code\|…`（不改产品码）；交付含**统一对齐表**：`ZGC 做法(file:line) \| 不变量 \| 我方对应(file:line 或"无") \| 差在哪`，每行落到 ✅等价 / ⚠形态不同 / ⛔真缺 三档之一 |
| **synthesize** | 交付含**实现任务书**（可直接派）+ 矛盾消解记录 |
| **implement** | DONE 时至少一条 `METHOD: test` 的 CLAIM（故意破坏转红，且**精确转红**，一破全红 = 过度捕获）；必须有 `## 产品接线证明`、`## 承重面清单`、`## 测试增删` |
| **review** | 必须明写「**放行**」或「**打回**」；放行时给两栏：**新的放进来什么 / 旧的挡住了什么** |
| **merge** | 必须给**逐文件内容核**的结果（`git grep -c <特征符号>`）；`git merge-tree` 报干净**对静默回退是盲的** |

## 5.1 出规格给别人用时：**标坐标基线**

⛔ 一份规格的 `file:line` 取自出规格者的私有工作树，被转派到另一棵树上 ⇒ 锚全部对不上，一整轮作废。

⇒ 规格里必须标一句 **「坐标基于 `<40 位 sha>`」**。
⇒ 转派方**必须先核锚在目标树上存不存在**，再派。

## 5.2 审查的**打回标准** —— A / B / C 三档

⛔ 实付：打回理由开始系统性地变成同一句「返工臂确已精确转红，**但未覆盖的 <某某入口> 断开后仍全绿**」⇒ 实现者补一个入口 ⇒ 审查再找出下一个 ⇒ **不收敛**。
⇒ **结构原因：实现者要覆盖【全部】入口，审查只需找到【一个】** —— 这是不对称的，对入口多的子系统没有终点。

```
A. 真缺陷 ⇒ 实现是错的（行为错 / 既有守卫或测试被削弱 / 违反裁决）
   ⇒ 一律打回，不论覆盖面如何

B. 枚举 / 记账 / 证据链不成立 ⇒ 它声明的方法本身会漏
   ⇒ 三种形态：① 方法够不到（虚派发/函数指针/模板/宏/非限定调用）
              ② 方法对但没跑遍每一面   ③ 外推
   ⇒ 也打回 —— 因为方法不修，下次还漏

   ⚠ **定责时的一个常见漏档：把「哪一笔提交引入」当成完整枚举，
     而漏掉「合并【动作本身】引入」。**
     实付：交付单独 343/343 绿、当时主线单独绿，**合起来 353/355** ——
     两侧各自绿 ⛔ 不等于合并后绿。合并是一个**变更点**。
     ⇒ 二分的端点要包含 merge commit 的**两个父**，不只是线性历史上的相邻提交。

C. 方法成立、但你靠额外洞察找到了第 N+1 个入口
   ⇒ 【不是】打回理由 ⇒ 记成 ## 后续项，放行本件
   ⇒ 若这也打回，只要入口够多，任何实现都永远过不了
```

⚠ **这不是放松** —— A 与 B 一条都不许放过，尤其 B：**「方法会不会漏」比「这次漏没漏」更值钱**。

⚠⚠ 但**也不许为了「显得认真」再找一条**。审查的价值在**证伪**，不在打回率。
若你只找到「还可以更严」的改进 ⇒ 那是 **C 类** ⇒ 记后续项、放行。

⚠⚠⚠ **A 与 C 的分界**（一条实付立的）：
```
若【是本提交自己】加的机制、并在契约里承诺了某个范围 ——
  那么「只兑现了一部分」= 没兑现自己写下的契约 ⇒ A 类
若那是别人留下的旧接口、你只是路过发现 ⇒ C 类
```

放行时（含带 `## 后续项` 的放行）仍要写两栏，并把 `## 后续项` 每条写成**可直接派的一句话**（入口 file:line + 为什么值得补）。

---

# §6 · 自检（**交付前跑一次**）

```bash
/root/cj_build/tools/cjops deliver check --lane <你的棒名>
```

⚠⚠⚠ **先说清楚这个检查【不能】做什么** —— 0824 用户令：

> 很多 gate 是启发式碰撞，不仅查不出想查的，**还会导致错误被隐藏**。

```
⭐ rc=0 只代表一件事：**报文的【形式】齐了**
   ⇒ 头五行位置对 · sha 是 40 位 · CLAIM 有 METHOD · measure 有 N · 必需的节都在
⛔ rc=0 【不】代表：
   ⛔ 你的 EVIDENCE 是真的        ⛔ 你的臂断对了地方
   ⛔ 你的枚举跑遍了每一面        ⛔ 你的三臂在同一个构型上
⇒ 那些**只能靠对抗审查**（它会自己挑一条你没验的去断）。
```

⚠ **别把 rc=0 写成「已合协议」向下游转述** —— 那正是「错误被隐藏」的机制。
正确说法：**「形式检查通过；实质由 review 判」**。

## 已被删掉的检查（**别加回来**）

0824 按实测删除了几条**宣称查内容、实测检出率为 0** 的规则。删除理由留在代码注释与测试里：

| 删掉的 | 它宣称查什么 | 实测 |
|---|---|---|
| 「`## 产品接线证明` 必须含产品树 `file:line`」 | 断线要断在**承重点** | 拿当年真正断错地方的三份交付回跑 ⇒ **0/3 命中**（它们都写得出坐标，只是断错了地方） |
| `PRIOR-ART`（关键词撞设计文档） | 仓里是否已有结论 | 最近 10 份任务书 **10/10 触发** ⇒ 零信息量 |
| `DIAG-UNUSED` | 已有仪器没被提到 | **10/10 触发** ⇒ 同上 |
| `NEGATIVE-PRIOR` | 该机制被判过阴性 | 8/10；命中形如「关键词 `Relocate.cpp` ⇒ 某报告」⇒ **文件名不是机制** |
| `CORE-DOMAIN` | 核域被别的棒占着 | **0/10**，触发条件要求早已不写的格式 ⇒ **结构性静默** |
| `adviseChannel`（措辞词表） | 预测会不会被分类器拦 | 9 样本手调的频次规则；且**禁止改措辞后，预测正确也无事可做** |

⇒ **总是响的门与从不响的门，危害是同一个**：它们占着「我查过了」那个位置，
让真正稀有的信号淹死在里面。

⇒ **立启发式门时要定期量触发率。** 报告语料越长，关键词碰撞越多 ——
这类门会**随时间退化成噪声**，不是立完就一直有效。

## 留下的门 —— 判据都是**结构性**的（读真实状态，不是文本碰撞）

| 门 | 它读什么 | 阳性对照 |
|---|---|---|
| `KKK2-DISK` | 远端真实 `df` | 降阈值即响 ✅ |
| `ANCHOR-MISMATCH` | 目标树真实 `git grep` + `git apply --check` | 阳性报、阴性静默 ✅ |
| `STALE-WORKTREE` | `merge-base --is-ancestor` + 已钉交付 | 有测试 ✅ |
| `CLASSIFIER-BLOCKED` | acpx 事件流的 `systemError` + 回绝措辞**共现** | 有测试 ✅ |
| `CANCELLED` | 事件流最后一条是 `session/cancel` | 有测试 ✅ |
| `REPORT-PATH` | 任务书里的报告路径与 lane 名是否一致 | 有测试 ✅ |

---

# 附录 A · 一个合协议的最小例子

```markdown
PROGRESS=DONE · verdict=P3-4 终止屏障本体在场，主控「只有观测」判错 · LANE=demo
DELIVERY_REF=none|no-code|对齐轮，只读
SIDE_EFFECT: 无
ROLE=explore
EVIDENCE=/root/cj_build/reports/REPORT-demo.md

## 对齐表
| ZGC | 不变量 | 我方 | 差在哪 |
|---|---|---|---|
| zMarkTerminate.inline.hpp:… | 所有 worker 无私有工作且共享栈空才可退出 | Mark.cpp:1404-1419 | ✅ 等价 |

## 主张
CLAIM: 我方终止屏障本体在场，不是只有观测计数
  METHOD: read
  EVIDENCE: runtime/src/Heap/Collector/Mark.cpp:1404-1419

## FALSIFIED
任务书称「屏障本体未落，只有 _ncontinue 观测」⇒ 源码证伪：
  观测在 TracingCollector.cpp:23-54，屏障本体在 Mark.cpp:1404-1419。
```

# 附录 B · 学费清单（每条规则的来源）

| 规则 | 实付 |
|---|---|
| §1.1 SIDE_EFFECT 必填 | 缺它的报告合过一次 ⇒ 0/20 → 20/20 |
| §1.1 sha 必须真实 | 报告里出现过编造的 full sha，收割时才发现 |
| §1.2 N 必填 | 主控一天内三次从 N=1~2 断言确定性，三次都被推翻 |
| §1.2 阳性对照 | 未武装的探针返回常量，据此发明了一整类不存在的缺陷 |
| §1.2 带 ELF sha | kkk2 上 24 个同名 `survival_dense`、13 个同名 `natural_wave_notime` |
| §2.1 产品接线证明 | 送审前 4 份实现，4 份全栽在「测试没穿过产品接线」 |
| §2.2 必须交产物 | 专为修这条派的返工件，仍交出「断线后全绿」 |
| §2.3 断在承重点 | 三条审查同时发现：三份都有红臂，三份都断在不承重处 |
| §3.1 机械枚举 | 五份都有真的臂，五份都漏掉别的承重接线 |
| §3.2 向上走到入口 | 一份交了 9 条臂 / 8 种形态仍被打回，枚举停在直接调用者 |
| §3.3 集合差 | 唯一的跨回绕用例被换成 ignored，而总数仍是 318 |
| §4.1 双构型 | 宏默认关 + 门无条件要求 ⇒ 谁都建不起来，且通过了审查 |
| §4.2 同构型 | 绿臂二进制建于最后一次源码改动之前 |
| §4.3 产生那一刻 | 门失败删掉 SO，事后重链的哈希绑不回被测文件 |
| §4.4 配方相同 | 两臂走不同构建入口 ⇒ 假集合差 311/390，同配方后是 44/0 |
| §5.1 标坐标基线 | 规格的 file:line 取自私有树，转派后锚全对不上 |
| §5.2 A/B/C | 送审 45 ⇒ 放行 1；打回理由不收敛 |
| §6 交付前自检 | 58 次打回只落在 22 条棒上，后几轮全是取证方式 |
