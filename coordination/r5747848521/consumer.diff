diff --git a/runtime/src/Heap/z/zMark.cpp b/runtime/src/Heap/z/zMark.cpp
index 653666ec0..130219fc0 100644
--- a/runtime/src/Heap/z/zMark.cpp
+++ b/runtime/src/Heap/z/zMark.cpp
@@ -270,7 +270,7 @@ void ZMark::VisitMinorRoots(const std::function<void(BaseObject*)>& visitor,
             visitor(object);
         });
         Heap::GetHeap().VisitAllExportRoots([&](NativeSlot& slot) {
-            ZBarrier::MarkBarrierOnOopField(slot, false);
+            /* P16 control: omit the second export-root barrier. */
             visitor(to_object(slot.GetTargetObject()));
         });
     }, (*Heap::GetHeap().GetZGeneration(ZGenerationId::young).Workers()).active_workers());
diff --git a/runtime/src/Heap/z/zRootsIterator.cpp b/runtime/src/Heap/z/zRootsIterator.cpp
index d06e22ac6..eba141fc8 100644
--- a/runtime/src/Heap/z/zRootsIterator.cpp
+++ b/runtime/src/Heap/z/zRootsIterator.cpp
@@ -136,8 +136,8 @@ void RootsIteratorStrongColored::Apply(const NativeSlotVisitor& visitor)
 void RootsIteratorAllColored::Apply(const NativeSlotVisitor& visitor)
 {
     NativeSlotVisitor copy = visitor;
-    strong.apply(&copy);
-    weak.apply(&copy);
+    /* P16 control: omit strong storage consumption. */
+    /* P16 control: omit weak storage consumption. */
     statics.apply(&copy);
 }
 
