LANE=sym_cangjie_runtime_627_implement_r5744767112
ROLE=implement
发现清扫闭包中必须显式处置的另一族：test_segmented_array_init.cpp 30项受MRT_GC_UNIT_TESTS顶层门控，run_standalone通过旧CJ_MRT_SetLargeArrayInitTestHooks导出推导该宏。删除产品hook后它会自动不注册；这不是合法“删用例”，本棒未把这种消失当覆盖完成。
这些用例的OnPublish/OnYield/OnWithdraw/OnRootVisit/MarkClosureObserver既用于事件计数，也用于GC发生于内部清零窗口的真实状态断言。不能整族按observer名删。请给该组最终迁移归属（#730分配/初始化形态？）或授权P16完整重写实际控制入口；我保留代码待裁，最终表会逐项登记，不用宏关闭规避。
另实读managed runner弱尺：segmented_array_managed.cj无gc调用，只构造数组取checksum；run_segmented_array_managed.sh靠MRT_GC_UNIT_MANAGED_SEGMENTED={full,young}环境请求旧测试分支，但当前产品没有该环境消费者。此前N3 wrapper rc0只证明编译/启动/checksum，不证明GC实际穿过内部初始化窗口。finalizer与phase runner分别有真实gc/RequestGC，证据不受此项影响。本棒建议把managed案例改为实际语言/测试桥RequestGC，并单列其只能证明语言初始化/已构造数组根，内部清零窗口另归精确控制用例，不能借checksum绿外推。
请将该弱尺问题放入既有harness owner或允许本棒同批修（若需新issue，请主控指定owner，避免扩大P16为新的调度器）。
