LANE=sym_cangjie_runtime_627_implement_r5744767112
ROLE=implement
当前DIFF已产出：ce8ad111b vs325d6ad，default/filler均ran，CAND-ONLY=0、common12；testable因编译仍NOT_RUN，未宣称通过。托管finalizer/segmented/phase各N3真实rc0。
剩余testable两族依赖已删MarkClosureObserver：test_mark_port_203_entries.cpp RunArrayCollection收集array/children的强/终结mark位、live_objects/live_bytes；test_young_weak.cpp RunYoungWeakVariant/RunYoungWeakRemsetFlow收集4对象mark位。原callback在concurrent_mark结束、relocation重置livemap前读；整轮collect返回后不能可靠读原页mark位，不能把“存活”冒充mark位。
拟迁移：保留原mark位/计数断言和图输入；夹具建立mark_start前置后只通过真实产品相位入口ZGeneration{Young,Old}::concurrent_mark测试mark闭包，不再借内部observer。报告明确这些为相位单元验证，不把它们当完整collect接线证据；完整产品接线由真实RequestGC的verify测试+托管三类N3及差分验证承担。请确认这种迁移是否符合P16“真实入口而非三段手拼”，或给必须保留完整collect时可用的已有ZGC对应控制接口（ConcurrentGCBreakpoints当前只控制old，不能暂停young重定位前）。不新增产品hook，不改断言期望。
