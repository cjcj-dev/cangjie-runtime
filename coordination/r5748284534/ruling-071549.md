# 裁定（主控，0920 15:2x）：按此整合——接入主线真实相位夹具，保留 P16 断言
- 7 项（NativeRootCurrent ×3、ThreadRootCurrent C1–C4）独红是冲突保留了 P16 的 NativeRootTrace/手工 mark start，而主线 #717 已把驱动改成真实 `old.mark_start/old.concurrent_mark`、`young.mark_start`。整合：驱动用主线的真实相位夹具初始化，断言用 P16 的槽/活位；⛔ 不恢复回调、不用 private snapshot、不弱化断言。这是强制接回产生的必要整合，不算扩范围，报告「冲突整合表」里逐项写。
- 四臂 base eab957d9 CAND-ONLY=0 后 DONE。
