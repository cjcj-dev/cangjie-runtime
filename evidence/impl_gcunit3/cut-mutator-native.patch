diff --git a/runtime/src/Mutator/Mutator.cpp b/runtime/src/Mutator/Mutator.cpp
--- a/runtime/src/Mutator/Mutator.cpp
+++ b/runtime/src/Mutator/Mutator.cpp
@@ -410,7 +410,6 @@ void Mutator::VisitStackRoots(const RootVisitor& func, const RootVisitor& invisi
     // incomplete large reference array can be published while a native helper
     // owns the mutator.
     if (!IsManagedContext()) {
-        VisitRawObjects(visitedInvisibleRootVisitor);
         MutatorUnlock();
         return;
     }
