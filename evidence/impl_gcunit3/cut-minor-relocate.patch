diff --git a/runtime/src/Heap/Collector/Relocate.cpp b/runtime/src/Heap/Collector/Relocate.cpp
--- a/runtime/src/Heap/Collector/Relocate.cpp
+++ b/runtime/src/Heap/Collector/Relocate.cpp
@@ -1290,6 +1290,7 @@ void WCollector::FixMinorRootSlots(const ScopedStopTheWorld* stw)
         NoteLargeArrayInitRootVisit(LargeArrayRootVisitSite::MINOR_RELOCATE,
                                     to_object(safe(root.LoadPlain(std::memory_order_acquire))));
 #endif
-        (void)FixMinorEvacuatedSlot(root, stw);
+        (void)root;
+        (void)stw;
     };
     DerivedPtrVisitor derivedVisitor = [this, stw](BasePtrType basePtr, DerivedSlot& derived) {
