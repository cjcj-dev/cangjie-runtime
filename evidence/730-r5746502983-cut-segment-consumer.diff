--- a/runtime/src/Heap/z/zObjArrayAllocator.cpp
+++ b/runtime/src/Heap/z/zObjArrayAllocator.cpp
@@ -142,7 +142,7 @@
     }
 #endif
     MArray* complete = static_cast<MArray*>(mutator->WithdrawInvisibleRoot());
-    complete->SetInvisibleObject(false);
+    complete->SetInvisibleObject(true);
     return complete;
 }
 
