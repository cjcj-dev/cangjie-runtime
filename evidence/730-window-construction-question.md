LANE=sym_cangjie_runtime_730_implement_r5746939673
坐标答复已收到，继续R1独立真实启发式输入。
R2/R3确定性窗口构造请裁：禁止恢复旧testPinnedPageAcquired等回调；diag_registry仅列既有MRT_GC_UNIT_MANAGED_SEGMENTED（当前在分段初始化内触发真实RequestGC），无 pinned获取后/GC边界窗口装置。拟新增测试构型下窗口驱动（无回调、只调真实RequestGC/同步线程，不代产品结果），并以独立诊断登记补丁交主控；R2在alloc_page返回后触发真实GC再让原ResetPageSequence/发布运行；R3在真实GC mark闭包既有SetMarkClosureObserver窗口调第二个真实Heap::alloc_page，核跨边界请求不误失败/下一波完成。是否准许R2扩展现有MRT_GC_UNIT_MANAGED_SEGMENTED的pinned模式（用途扩大），或应采用外部gdb断点在获取后调测试驱动以避免新产品测试接线？R3优先复用既有mark observer，不新增回调。
