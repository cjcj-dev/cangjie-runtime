--- a/runtime/src/Heap/z/zBarrier.cpp
+++ b/runtime/src/Heap/z/zBarrier.cpp
@@ -285,7 +285,7 @@
         return address;
     }
     if (young.IsMajorRoots()) {
-        theCollector.GetGenerationCycle(GCCycleGeneration::OLD).MarkObject<false, true, true, false>(address);
+        // P2 regression: omit major-roots old publication.
         return address;
     }
     return address;
