diff --git a/runtime/src/Heap/z/zBarrier.cpp b/runtime/src/Heap/z/zBarrier.cpp
index f412b6485..a3f729505 100644
--- a/runtime/src/Heap/z/zBarrier.cpp
+++ b/runtime/src/Heap/z/zBarrier.cpp
@@ -293,7 +293,7 @@ void ZBarrier::WriteReference(BaseObject* obj, RefField<false>& field, BaseObjec
 {
     const bool weakReferent = obj != nullptr && Heap::IsHeapAddress(obj) && obj->IsWeakRef() &&
         reinterpret_cast<MAddress>(&field) == reinterpret_cast<MAddress>(obj) + TYPEINFO_PTR_SIZE;
-    StoreBarrier(obj, field, false, weakReferent ? ReferenceStrength::Weak : ReferenceStrength::Strong);
+    (void)weakReferent;
     WriteReferenceImpl(obj, field, ref);
 }
 
diff --git a/runtime/src/Heap/z/zGeneration.cpp b/runtime/src/Heap/z/zGeneration.cpp
index e49a9bef8..9628a1077 100644
--- a/runtime/src/Heap/z/zGeneration.cpp
+++ b/runtime/src/Heap/z/zGeneration.cpp
@@ -588,7 +588,7 @@ void ZGenerationYoung::concurrent_mark()
     }
     {
         ZStatTimerYoung zstatTimer(PYoungRemsetRescan);
-        Heap::GetHeap().remembered().scan_and_follow(Heap::GetHeap().young().MarkPtr());
+        // P2 cut: omit real remset consumer.
     }
 #if defined(MRT_TESTABLE_INTERNALS)
     // Deterministic T1->T2 export-root window: root enumeration has returned,
