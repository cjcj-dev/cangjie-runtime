新增真实MCC_GetParameterAnnotations→ApplyCangjieMethodStub→注解callback→RequestGC(YOUNG,false)用例实测暴露前提缺口：注解包装通过Handle→AddNativeFrameRoot登记，GC选择4/4小页，存活12320 bytes；返回后VisitMutatorRoots读nativeFrameRoots仍是before=after=40000000000，返回obj同值。四个注解入口同样。未把它当PASS，目标断言before!=after准确FAIL。日志kkk2:/root/sym_cangjie_runtime_581_implement_r5748887475_annotations/default/single.log；SO d2a041bcc56a772a4c40dddb88d123ad95e5b9001e11ead0e831d0729894b418。既有RawHolderExcludesCollectionRelocation同SO PASS（export roots路线）。
源码Mutator::PreForwardRoots约915-963有native根重新取址，StackWatermark::process_head:78也扫描；但Mutator::DoLeaveSaferegion仅Handshake::leave_safe，RequestGC返回是否实际触发relocate native roots需要继续定位。不能给Handle::operator()偷偷加barrier或测试手工调用PreForwardRoots来补绿，那将不符合HotSpot Handle形态。
请确认：如果实证是既有nativeFrameRoots消费端迁移缺口，可否纳入本轮最小修复？或主控已有相关在飞项？我继续其他调用者迁移并定位执行结果，不等答案空转。
