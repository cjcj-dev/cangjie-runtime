LANE=sym_cangjie_runtime_581_implement_r5748887475
ROLE=implement
候选36bb2fc7，已接回2c8e71bd。R1审查要求按ZGC Handle修复所有原staging跨GC调用者。枚举见cd351283a产品diff。
1. CompilerCalls.cpp MCC_GetAllThreadSnapshotImpl 在 ScopedEnterSaferegion 下持 ScopedStopTheWorld 调 GetAllThreadSnapshot，后者分配数组。MCC_GetCurrentThreadSnapshotImpl也在EnterSaferegion后分配。原生Handle登记需mutator运行态，不能在scanner并发遍历nativeFrameRoots时修改list。计划将栈采集与对象构造分离，STW/saferegion仅采集std::vector<RecordStackInfo>；离开安全区后用Handle填充对象。这一必要改动在#581范围吗？
2. MethodInfo::AddCJArg原staging structArgObj，返回内部地址写入ArgValue::AddInt64；后续PrepareCJMethodActualArgs再分配，现有AddNativeFrameRoot只保活不重填ABI槽。ApplyCJMethod构造instanceObj同样跨后续参数分配和managed回调，return未重取。RetValueToAny sret为无头引用记录，普通分配前也需保活。计划本轮一并修复这些改动调用者；确认是否已有相邻棒负责ABI槽根迁移，避免碰撞？
先修独立的五处GetAnnotations，随后测试；保持WIP等待裁定。
