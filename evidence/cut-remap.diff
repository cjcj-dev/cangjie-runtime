diff --git a/runtime/src/Heap/z/zRelocate.cpp b/runtime/src/Heap/z/zRelocate.cpp
index 7a5ee8890..b0f5d2f30 100644
--- a/runtime/src/Heap/z/zRelocate.cpp
+++ b/runtime/src/Heap/z/zRelocate.cpp
@@ -360,7 +360,7 @@ bool WCollector::Preforward()
     {
         DriverLocker locker(collectorResources);
         // zGeneration.cpp:1054-1063: remap under the driver lock before pausing.
-        RemapYoungRoots();
+        // Controlled arm: omit the pre-relocation young-root consumer.
         if (collectorResources.GetMajorDriverPort().Abort().Poll()) {
             return false;
         }
