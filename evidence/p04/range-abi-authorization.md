LANE=sym_cangjie_runtime_610_implement_r5687426297
ROLE=synthesize
202002Z反证成立：不得用ReservedAddressSpan/包围区间替代P01真实段，也不得恢复已退出的ReservationRegistry。接受初始化期最小适配：在任何运行期claim/free或并发参与者出现前，遍历全部实际分区，以现有remove_from_low_many_at_most暂取完整空闲ranges，按同一管理器insert归还；临时ZArray只用于形成UnitSegment/逆向metadata/Heap::OnHeapCreated真实段输入，不保留第二份运行期注册表。须证明操作前后可用字节数与地址集合相同、未漏分区/孔洞；若插入合并相邻段，只可在真实连续已保留地址的并集上合并，不能跨未保留间隙。保留P06正式ZPerNUMA当前count=1，不能写死绕过其API。

100与8是实际跨仓ABI缺口，不通过降低ZMaxVirtualReservations、放宽CHECK或只修改runtime一侧解决。授权P04同包增加一个狭窄LLVM配套：runtime g_cjHeapRangeStart/End容量覆盖ZMaxVirtualReservations=100，count发布边界保持明确；LLVM的声明/访问同步此真实容量，并按实际g_cjHeapRangeCount进行有界访问，避免机械展开100个无效槽。保留无孔洞误判和count上界检查，0段/1段/第9段/>8段及边界/间隙都有针对性证明。原8槽生成器不能被说成已经支持第9段。

LLVM权威/root/cj_build/llvm_rebase，基线main1a01451912f160219665abdc497274e574338bad；独立工作树/root/cj_build/llvm_rebase_wt/sym_cangjie_runtime_610_implement_r5687426297，分支sym/610-range-abi-llvm-r5687426297，先核未存在，不动主树/原SDK。写集仅保留域表声明/查询lowering及其必要IR/真实consumer测试，不接管P08完整屏障。P08暂不改emitReservedHeapSlot/kCjHeapRangeCap这个ABI面，后续消费本包配套；同一CJBarrierLowering.cpp的其它函数按hunk并行。

这是对P01真实段合同在新ZGC manager下的完整保留，不是新增兼容旁路。交runtime+LLVM两个精确候选/独立PR，联合Review后协调hold，由唯一merge角色联合内容核/合入/回读才统一Done；不允许通用单仓Merge先关闭#610。P04实现者不push，由独立publisher代推。保留旧P04候选/原证据；新>8真实段与LLVM读/写consumer须实际经过新候选，旧P01两段臂不能独自证明扩展。

独立容器/初始化适配可继续。当前大复制/大构建仍遵守最新资源回执，LLVM源码分支可本地建立；新LLVM代码需新产物，不能把旧P01 llc改标。若具体有界IR循环必须修改调用者CFG，限定在该helper实际调用者，交对应IR验证，不趁机重排P08其它屏障。
