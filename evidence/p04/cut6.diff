diff --git a/runtime/src/Heap/z/zPageAllocator.cpp b/runtime/src/Heap/z/zPageAllocator.cpp
index 81f13cbc0..b93e55e02 100644
--- a/runtime/src/Heap/z/zPageAllocator.cpp
+++ b/runtime/src/Heap/z/zPageAllocator.cpp
@@ -335,7 +335,7 @@ bool FreeRegionManager::PreparePageMemory(PageMemory& memory)
     // Restore segments
     if (!result.is_null()) {
         // Got exact match. Restore stashed physical segments for the harvested part.
-        restore_segments(result.first_part(harvested), stash);
+        // Control: disconnect the physical-stash consumer.
     } else {
         // Got many partial vmems
         restore_segments(memory.partialMappings, stash);
