diff --git a/runtime/src/Heap/z/zPageAllocator.cpp b/runtime/src/Heap/z/zPageAllocator.cpp
index 7738a2e04..1957f9a83 100644
--- a/runtime/src/Heap/z/zPageAllocator.cpp
+++ b/runtime/src/Heap/z/zPageAllocator.cpp
@@ -484,7 +484,7 @@ void FreeRegionManager::UncommitFlushed(size_t flushed)
         const size_t part = std::min(remaining, partition->claimed);
         if (part == 0) { continue; }
         partition->claimed -= part;
-        decrease_capacity(partition->numaId, part, false /* set_max_capacity */);
+        // RED ARM 3: capacity account not decreased after uncommit
         remaining -= part;
     }
     CHECK(remaining == 0);
