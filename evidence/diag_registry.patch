--- a/diag_registry.py
+++ b/diag_registry.py
@@ -27,8 +27,5 @@
 MAP = r'''
+校验|verify|roots|objects|marking|remembered|forwarding|ZVerifyRoots,ZVerifyObjects,ZVerifyMarking,ZVerifyRemembered,ZVerifyForwarding,ZVerifyOops|ZGC 正式验证选项；默认值镜像 z_globals.hpp，ZVerifyOops 仅 debug 构型可配置
 挂起|hang|HANG|活锁|死锁|livelock|deadlock|spin|MRT_GC_LOG|⭐⭐⭐ 每次收集/每相位一条记录 ⇒ 多少轮、每轮进展多少、什么在重复触发（⛔ 别先上 gdb —— gdb 只给两张快照，⭐ 日志给整条时间序列）
-young|minor|疏散|evac|晋升|MRT_GCV2_MINOR_DEFERS_HEU|⭐ ACTIVE：minor 回滚对照臂。YOUNG_CONC_MARK/FOLLOW、YOUNG_MARK_END_FORCE_REENTER、EPOCH_HANDSHAKE getenv 已随 mc 翻默认删除（恒开）
-remset|跨代|cross-gen|着色|颜色|colour|color|带色|剥色|root|根|栈图|stackmap|MRT_GCV2_REMSET_HASH_OPT,MRT_GCV2_VERIFY_ROOTS,MRT_GCV2_VERIFY_HEAP,MRT_GCV2_VERIFY_OBJECTS,MRT_GCV2_VERIFY_MARKING,MRT_GCV2_VERIFY_REMSET,MRT_GCV2_VERIFY_REMEMBERED,MRT_GCV2_VERIFY_REGIONS,MRT_GCV2_VERIFY_OOPS,MRT_GCV2_VERIFY_STACK_ROOTS_COMPLETE,MRT_GCV2_STACK_WATERMARK_VERIFY,MRT_GCV2_REMSET_EVER,MRT_GCV2_STATHEAL_NOHEAL,MRT_GCV2_VERIFY_PAGE_OWNER|⭐ ACTIVE（源码真实 getenv，可 export）：VerifyFace 五面、MRT_GCV2_DIAG token、remset / stack-root / watermark 校验与回滚对照臂（MARKCOMPLETE getenv 已随 #203 M3 删除）
-并行疏散|evacuation gate|MRT_GCV2_EVACPAR_WORK_GATE|⭐ ACTIVE（源码真实 getenv，可 export）：evacuation worker gate 对照臂
 归还|uncommit|所有权|MRT_UNCOMMIT_CUT_OWNERSHIP|⭐ ACTIVE（源码真实 getenv）：uncommit 所有权断线对照臂（CartesianTree.cpp:153 / Uncommitter.cpp:149）
-并行 pinned|pinned scan|MRT_GCV2_PINNED_SCAN_PARALLEL|⭐ ACTIVE（源码真实 getenv，可 export）：pinned 扫描并行/串行对照臂
 日志|log level|日志等级|MRT_LOG_FILE_SIZE,MRT_LOG_LEVEL,MRT_REPORT|⭐ ACTIVE（源码真实 getenv；MRT_REPORT 在 OHOS 分支）：日志文件大小、等级与报告路径控制
@@ -36,8 +33,3 @@
 分段堆|managed segmented|单测构型|MRT_GC_UNIT_MANAGED_SEGMENTED,MRT_GC_UNIT_RUNTIME_MUTATOR_LIFECYCLE_ONLY|⭐ ACTIVE（源码真实 getenv，可 export）：managed segmented / mutator-lifecycle-only GC unit 构型选择（MutatorManager.cpp:249）
-握手|epoch|MRT_GCV2_EPOCH_HANDSHAKE_TIMEOUT_MS|⭐ ACTIVE：握手超时剂量。握手开关本身已恒开无 getenv
-标记水位|mark water|MRT_GCV2_MARKWATER_OFF|⭐ REMOVED（#203 M3）：mark-water 恒开，无 getenv
-预翻转|preflip|残留|residue|MRT_GCV2_PREFLIP_VERIFY,MRT_GCV2_PREFLIP_VERIFY_FATAL|⭐ krestore K 真 getenv，可 export：恢复 preflip 保险遍历；FATAL 仅在发现 residue 时中止
-并发扫栈|concurrent stack|handshake|握手|MRT_GCV2_EPOCH_HANDSHAKE_TIMEOUT_MS|⭐ 真 getenv：握手超时剂量。并发栈扫描已恒开无 getenv
 性能|吞吐|开销|cost|throughput|MRT_PHASE_TIMER|⭐ 分项开销（LEDGER_COST/SCRUB_COST 探针已删；计时用 task-clock）
-并行度|workers|serial|MRT_GCV2_JVM_GC_THREADS,MRT_GCV2_STWPAR_WORKERS,MRT_GCV2_STWPAR_FORCE_SERIAL,MRT_GCV2_EVACPAR_WORKERS,MRT_GCV2_EVACPAR_FORCE_SERIAL,MRT_GCV2_REFFIX_WORKERS,MRT_GCV2_REFFIX_FORCE_SERIAL|⭐ 真 getenv；MARKPAR_WORKERS/FORCE_SERIAL 已随 #203 M3 删除（young trace 恒 stripe）
 相位|pause|停顿|并发窗|zstat|五柱|pillar|MRT_ZSTAT|⭐ zstat 棒（0823）移植 ZStatPhase 一族：[ZSTAT] rec=zphase/rec=zcycle 按 pause/concurrent 分账 + max_pause_ns；消费侧 tools/zstat_pillars.py；单元 runtime/tests/gc_unit/test_zstat.cpp
@@ -345,3 +337,2 @@
 # [sh原注] #   ⭐ `flagcut` 把 runtime 的 190 个 env 开关砍到 **3 个**（另有 3 个非 GC 的）；
-# [sh原注] #   ⭐ `fyscut` 把 `MRT_GCV2_FULL_YOUNG_SCAN` **整个删掉**。
 # [sh原注] #   ⇒ ⭐⭐ 而 MAP 是**手维护的名单** ⇒ ⭐ 砍完的第一次派发（`pubwindow`），
@@ -370,3 +361,2 @@
 # [sh原注] #      （⭐ marker 在哪个文件 · ⭐ 那文件多少行）⇒ ⭐⭐ 让读的人自己看,⛔ 别替他判
-# [sh原注] #   ⚠ ⭐ 实账:⭐ `MRT_GCV2_STACK_WATERMARK_VERIFY` 的 marker 在
 # [sh原注] #     `UnwindStack/StackWatermark.cpp`(**23 行 = 空桩**)⇒ ⭐⭐ 改编译期常量**开不出来**,
@@ -381,3 +371,2 @@
 # [sh原注] # ⛔⛔ 为什么 `check` 不够：⭐ 它只在**任务书恰好提到那个关键词**时才核那一行
-# [sh原注] #   ⇒ ⭐⭐ 从没被问到的行可以陈旧任意久（⭐ `MRT_GCV2_YOUNG_MARK_END_REENTER`
 # [sh原注] #      ⭐ 这个名字**从来就不存在**，⭐ 真名是 `_FORCE_REENTER`，⭐ 而它在表里躺了很多天）。
@@ -395,6 +384,4 @@
 # [sh原注]       # ⚠⚠ ⭐⭐⭐ 三态，⛔ 不是两态 —— ⭐ 0825 实付，⭐ 这把尺我差点用错：
-# [sh原注]       #   ⭐ `MRT_GCV2_CONCURRENT_STACK_SCAN` 有**活的 getenv**（MutatorManager.cpp），
 # [sh原注]       #   ⭐⭐ 但 `strings | grep -x` **0 命中** ⇒ ⭐ 我差点判它「被钉死」。
 # [sh原注]       #   ⭐⭐⭐ 真相：⭐ 同 TU 里那条日志格式串**正好以这个名字结尾**
-# [sh原注]       #     （`… env_scan=MRT_GCV2_CONCURRENT_STACK_SCAN`）⇒ ⭐⭐ 字符串尾部合并
 # [sh原注]       #     ⇒ ⭐ getenv 的实参指到长串内部 ⇒ ⭐⭐ `strings` 只吐得出那条长的。
@@ -403,3 +390,2 @@
 # [sh原注]   # ⭐⭐⭐⭐ 0826 加：**反方向** —— ⭐ 源码里有 getenv，⭐⭐ 而名册里没登记
-# [sh原注]   #   ⚠ ⭐⭐⭐ 实付：⭐ 主控为了让 flipdel 合成树过关，⭐⭐ 从 MAP 删掉 MRT_GCV2_MINOR_YOUNG_FLIP
 # [sh原注]   #     ⇒ ⭐ 两臂立刻都绿 ⇒ ⭐⭐⭐ 而**主线上那个开关还在、变成无人登记**
