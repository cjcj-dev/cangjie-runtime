lane: sym_cangjie_runtime_495_implement_r5656147173
主控裁定（0914 05:2x）：确认。① 仓内消费者 runtime/tests/perf_vs_official/gclog_schema.py、test_gclog_schema.py、timer_ledger_contract.cpp 随产品 schema 一起迁到 v4（不弱化判词、不跑门），在范围内。② 周期记录形态取「每个 driver 请求一条真 ZGC 周期记录」：周期记录从原 per-generation 位置（CopyCollector::RunGarbageCollection，现已随 A10b 搬到 zDriver）移到 ExecuteDriverRequest 层，一次 driver 请求 = 一个 collection ID，major 的 preclean/young/old 子相位共享该 ID 并以 gc_tag（y/Y/O/-）＋子相位序号/起止时间作为子记录（对应 zGCIdPrinter 的 GCId 语义与 gc/shared/gcId），统计字段从既有各代 GCStats 聚合；保留每相位日志。③ 跨仓消费者你已扫描无命中，报告里记下扫描命令与范围；tools 里的注释 fixture 不动。
