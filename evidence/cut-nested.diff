diff --git a/runtime/src/Heap/z/zRelocate.cpp b/runtime/src/Heap/z/zRelocate.cpp
index 3117f623b..1ae139ea2 100644
--- a/runtime/src/Heap/z/zRelocate.cpp
+++ b/runtime/src/Heap/z/zRelocate.cpp
@@ -321,7 +321,7 @@ void WCollector::RemapYoungRoots()
         // their plain fields before remapping, as verification and mark do.
         // ZGC zStackWatermark.cpp:164-214 processes each oop frame slot.
         RootVisitor heapRoots = [&](ObjectRef& root) {
-            mutator.VisitHeapRootSlots(root, visitor);
+            visitor(root); // Controlled arm: omit nested stack-root expansion.
         };
         DerivedPtrVisitor derived = Mutator::MakeDerivedRootVisitor(visitor);
         size_t frames = 0;
