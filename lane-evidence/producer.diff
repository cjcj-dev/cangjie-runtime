diff --git a/runtime/src/Heap/z/zHeap.cpp b/runtime/src/Heap/z/zHeap.cpp
index cedfc6231..1e3f5a962 100644
--- a/runtime/src/Heap/z/zHeap.cpp
+++ b/runtime/src/Heap/z/zHeap.cpp
@@ -503,7 +503,7 @@ ZPage* Heap::alloc_page(size_t num, ZPageType role, bool expectPhysicalMem, bool
         page = manager.TakeRegion(num, role, expectPhysicalMem, allowSaferegion, clearPayload, age);
     }
     if (page != nullptr) {
-        page_table().insert(page);
+        // Controlled producer cut: omit publication.
     }
     return page;
 }
